import 'dart:ffi';

import 'package:flutter/cupertino.dart';

const EdgeInsets DIM_APP_CONTAINER = EdgeInsets.only(right: 10.0, left: 10.0);
const EdgeInsets DIM_VTYPE_CONTAINER = EdgeInsets.only(top: 5.0, bottom: 5.0);
const double CAR_WIDTH = 100;
const double CAR_HEIGHT = 100;
